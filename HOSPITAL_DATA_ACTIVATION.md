# 병원 기본정보 API 활성화 메모

기준일: 2026-08-15

## 목적

`/api/hospitals`는 건강보험심사평가원 병원정보서비스의 기관 기본정보만 운정 생활권으로 좁혀 반환한다. 진료시간, 접수 마감, 야간·휴일 운영은 반환하거나 추정하지 않는다.

## 현재 상태

- 서버 경로는 배포 대상에 포함한다.
- 개인 개발계정의 활용신청이 확인되기 전에는 `official-source` 응답만 반환한다.
- 화면은 API 결과를 전제로 하지 않으며, E-Gen·파주시 보건소·의료기관 전화 확인 경로를 계속 우선 표시한다.

## 활성화 순서

1. 공공데이터포털에서 [건강보험심사평가원 병원정보서비스](https://www.data.go.kr/data/15001698/openapi.do)를 개인 개발계정으로 활용신청한다.
2. Cloudflare Pages Production 환경 변수 `DATA_GO_KR_SERVICE_KEY`에 일반 인증키를 저장한다.
3. `https://analogblow.com/api/hospitals?rows=5`가 `mode: live`와 기관 목록을 반환하는지 확인한다.
4. 실제 응답이 확인된 뒤에만 `/health`와 `/map-search`에 목록 UI를 추가한다.

## 실패 처리

- 응답이 비어 있거나 권한 오류면 기관 목록을 표시하지 않는다.
- 사용자는 E-Gen, 파주시 보건소, 의료기관 전화로 이동할 수 있어야 한다.
- 서비스 키와 원본 API 요청 URL은 브라우저 응답과 저장소에 노출하지 않는다.
